# Porta do Postgres no HOST (padrão 5432).
# Se a 5432 já estiver ocupada na sua máquina, troque aqui E na DATABASE_URL (ex.: 5433).
DB_PORT=5432
DATABASE_URL="postgresql://echo:echo@localhost:5432/echo?schema=public"

# Porta da API
PORT=3333
